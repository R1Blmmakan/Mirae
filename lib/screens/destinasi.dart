import 'package:flutter/material.dart';


class DestinasiPage extends StatelessWidget {
  const DestinasiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Destinasi'),
      ),
      body: const Center(
        child: Text('Halaman Destinasi'),
      ),
    );
  }
}